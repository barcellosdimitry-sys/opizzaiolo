<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rodízio de Pizzas Artesanais | Foz do Iguaçu</title>
    <!-- Google Fonts: Fonte Única (Montserrat) -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-primary: #0f0e0d;
            --bg-card: rgba(22, 20, 18, 0.85);
            --accent: #e5a93b;
            --accent-hover: #f1b74a;
            --text-main: #f4f4f4;
            --text-muted: #b8b5b0;
            --font-family: 'Montserrat', sans-serif;
            --shadow-subtle: 0 12px 35px rgba(0, 0, 0, 0.7);
            --border-glow: rgba(229, 169, 59, 0.25);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: var(--font-family);
        }

        body {
            background-color: var(--bg-primary);
            /* Fundo único contínuo com textura sutil em gradiente para toda a página */
            background-image: 
                radial-gradient(circle at 20% 15%, rgba(229, 169, 59, 0.05) 0%, transparent 40%),
                radial-gradient(circle at 80% 85%, rgba(217, 79, 39, 0.05) 0%, transparent 40%),
                linear-gradient(180deg, #0f0e0d 0%, #161310 50%, #0d0c0b 100%);
            color: var(--text-main);
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* --- HERO / CAPA --- */
        .hero {
            position: relative;
            min-height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(180deg, rgba(15,14,13,0.3) 0%, rgba(15,14,13,0.85) 90%, var(--bg-primary) 100%), 
                        url('https://images.unsplash.com/photo-1513104890138-7c749659a591?auto=format&fit=crop&w=1920&q=80') no-repeat center center/cover;
        }

        .hero-content {
            max-width: 800px;
            z-index: 2;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4.2rem);
            font-weight: 800;
            letter-spacing: -1px;
            margin-bottom: 20px;
            text-shadow: 0 4px 15px rgba(0,0,0,0.8);
            color: #ffffff;
        }

        .hero h1 span {
            color: var(--accent);
        }

        .hero p {
            font-size: clamp(1.1rem, 2vw, 1.35rem);
            color: var(--text-muted);
            margin-bottom: 35px;
            line-height: 1.6;
            text-shadow: 0 2px 8px rgba(0,0,0,0.8);
        }

        /* --- BOTÕES E CTAS --- */
        .btn {
            display: inline-block;
            background: linear-gradient(135deg, #e5a93b 0%, #c68a26 100%);
            color: #111;
            font-weight: 700;
            padding: 16px 36px;
            font-size: 1.1rem;
            border-radius: 50px;
            text-decoration: none;
            box-shadow: 0 8px 25px rgba(229, 169, 59, 0.35);
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn:hover {
            transform: translateY(-3px);
            background: linear-gradient(135deg, #f1b74a 0%, #d4952e 100%);
            box-shadow: 0 12px 30px rgba(229, 169, 59, 0.5);
        }

        /* --- SEÇÕES GERAIS --- */
        section {
            padding: 80px 0;
            position: relative;
        }

        .section-title {
            text-align: center;
            font-size: clamp(2rem, 3.5vw, 2.8rem);
            font-weight: 700;
            margin-bottom: 15px;
            letter-spacing: -0.5px;
        }

        .section-subtitle {
            text-align: center;
            color: var(--text-muted);
            font-size: 1.15rem;
            max-width: 650px;
            margin: 0 auto 55px auto;
            line-height: 1.6;
        }

        /* --- DESTAQUES DO CARDÁPIO (Ajuste de grade e fixidez visual) --- */
        .highlights-wrapper {
            display: grid;
            grid-template-columns: 1fr 1.2fr;
            gap: 40px;
            align-items: center;
            background: var(--bg-card);
            border: 1px solid var(--border-glow);
            border-radius: 24px;
            padding: 40px;
            box-shadow: var(--shadow-subtle);
            position: relative;
            /* Sombreamento e textura na base do bloco */
            border-bottom: 5px solid rgba(229, 169, 59, 0.4);
        }

        @media (max-width: 900px) {
            .highlights-wrapper {
                grid-template-columns: 1fr;
                padding: 25px;
            }
        }

        .highlights-img-container {
            position: relative;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0,0,0,0.5);
            height: 100%;
            min-height: 380px;
        }

        .highlights-img-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
            transition: transform 0.5s ease;
        }

        .highlights-img-container:hover img {
            transform: scale(1.03);
        }

        .highlights-list {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .highlight-item {
            background: rgba(28, 25, 22, 0.7);
            border-left: 4px solid var(--accent);
            padding: 20px 22px;
            border-radius: 0 12px 12px 0;
            box-shadow: 0 4px 15px rgba(0,0,0,0.4);
            transition: transform 0.2s ease;
        }

        .highlight-item:hover {
            transform: translateX(5px);
            background: rgba(35, 31, 27, 0.9);
        }

        .highlight-item h3 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 6px;
            color: #fff;
        }

        .highlight-item p {
            color: var(--text-muted);
            font-size: 0.98rem;
            line-height: 1.5;
        }

        /* --- BLOCOS DE EXPERIÊNCIA / DIFERENCIAIS --- */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .feature-card {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 35px 30px;
            box-shadow: var(--shadow-subtle);
            position: relative;
            overflow: hidden;
            border-bottom: 4px solid rgba(229, 169, 59, 0.2);
            transition: transform 0.3s ease, border-color 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            border-color: var(--accent);
        }

        .feature-card h3 {
            font-size: 1.4rem;
            margin-bottom: 12px;
            font-weight: 600;
            color: #fff;
        }

        .feature-card p {
            color: var(--text-muted);
            line-height: 1.6;
            font-size: 1rem;
        }

        /* --- PERGUNTAS FREQUENTES (Resolvido o bug de arrasto em mobile antigo) --- */
        .faq-section {
            clear: both; /* Assegura isolamento estrutural completo contra bugs de fluxo em motores antigos */
            width: 100%;
            margin-top: 40px;
            padding-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .faq-accordion {
            max-width: 800px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .faq-item {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.06);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        }

        .faq-question {
            width: 100%;
            background: none;
            border: none;
            text-align: left;
            padding: 20px 25px;
            font-size: 1.1rem;
            font-weight: 600;
            color: #fff;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .faq-question::after {
            content: '+';
            font-size: 1.5rem;
            color: var(--accent);
            transition: transform 0.3s ease;
        }

        .faq-item.active .faq-question::after {
            transform: rotate(45deg);
        }

        .faq-answer {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out, padding 0.3s ease;
            padding: 0 25px;
            color: var(--text-muted);
            font-size: 1rem;
            line-height: 1.6;
        }

        .faq-item.active .faq-answer {
            max-height: 200px;
            padding: 0 25px 20px 25px;
        }

        /* --- RODAPÉ --- */
        footer {
            text-align: center;
            padding: 50px 20px;
            color: var(--text-muted);
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            background-color: #0b0a09;
            font-size: 0.95rem;
        }

        footer span {
            color: var(--accent);
            font-weight: 600;
        }
    </style>
</head>
<body>

    <!-- HERO / CAPA -->
    <header class="hero">
        <div class="hero-content">
            <h1>A Autêntica Experiência da <span>Pizza Artesanal</span> em Foz do Iguaçu</h1>
            <p>Massa de fermentação natural, ingredientes selecionados e sabores inesquecíveis em um rodízio exclusivo feito para surpreender você e sua família.</p>
            <a href="https://wa.me/seunumero" target="_blank" class="btn">Garantir Minha Reserva no WhatsApp</a>
        </div>
    </header>

    <main class="container">
        
        <!-- DESTAQUES DO CARDÁPIO -->
        <section>
            <h2 class="section-title">Destaques do Cardápio</h2>
            <p class="section-subtitle">Conheça as combinações mais amadas da nossa casa, preparadas artesanalmente para entregar crocância e cremalheira perfeitas.</p>
            
            <div class="highlights-wrapper">
                <div class="highlights-img-container">
                    <img src="https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?auto=format&fit=crop&w=800&q=80" alt="Pizza Artesanal Destaque">
                </div>
                <div class="highlights-list">
                    <div class="highlight-item">
                        <h3>Linguiça Artesanal com Provolone</h3>
                        <p>Linguiça artesanal levemente defumada, generosas camadas de provolone derretido e toque especial de ervas finas.</p>
                    </div>
                    <div class="highlight-item">
                        <h3>Quatro Queijos com Mel Rústico</h3>
                        <p>Mozarela, gorgonzola cremoso, parmesão maturado e provolone, finalizados com um fio de mel silvestre artesanal.</p>
                    </div>
                    <div class="highlight-item">
                        <h3>Margherita Especial da Casa</h3>
                        <p>Molho de tomates rústicos italianos, mozarela de búfala fresca, manjericão colhido na hora e azeite extravirgem.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- DIFERENCIAIS -->
        <section>
            <h2 class="section-title">Por Que Nos Escolher?</h2>
            <p class="section-subtitle">Muito mais que uma refeição: um momento único pensado em cada detalhe para você.</p>
            
            <div class="features-grid">
                <div class="feature-card">
                    <h3>Fermentação Lenta</h3>
                    <p>Nossa massa descansa por horas antes de ir ao forno, resultando em uma textura leve, de fácil digestão e bordas incrivelmente crocantes.</p>
                </div>
                <div class="feature-card">
                    <h3>Ingredientes Premium</h3>
                    <p>Trabalhamos exclusivamente com insumos selecionados de alta qualidade, garantindo frescor e sabor marcante em cada fatia.</p>
                </div>
                <div class="feature-card">
                    <h3>Ambiente Acolhedor</h3>
                    <p>Espaço planejado para receber você, sua família e amigos com conforto, atendimento ágil e uma atmosfera inconfundível em Foz.</p>
                </div>
            </div>
        </section>

        <!-- PERGUNTAS FREQUENTES (Com isolamento robusto para dispositivos móveis) -->
        <section class="faq-section">
            <h2 class="section-title">Perguntas Frequentes</h2>
            <p class="section-subtitle">Tire suas dúvidas rapidamente antes de garantir sua mesa.</p>
            
            <div class="faq-accordion">
                <div class="faq-item">
                    <button class="faq-question">É necessário fazer reserva antecipada?</button>
                    <div class="faq-answer">
                        Recomendamos fortemente a reserva prévia via WhatsApp para garantir seu lugar sem filas, especialmente nos finais de semana.
                    </div>
                </div>
                <div class="faq-item">
                    <button class="faq-question">Quais os horários de funcionamento do rodízio?</button>
                    <div class="faq-answer">
                        Atendemos de terça a domingo, das 18h30 às 23h30. Venha aproveitar com tranquilidade!
                    </div>
                </div>
                <div class="faq-item">
                    <button class="faq-question">Possuem opções vegetarianas ou restrições?</button>
                    <div class="faq-answer">
                        Sim! Contamos com pizzas vegetarianas deliciosas em nosso cardápio rotativo para atender a todos os gostos.
                    </div>
                </div>
            </div>
        </section>

    </main>

    <!-- RODAPÉ -->
    <footer>
        <p>&copy; 2026 <span>Rodízio de Pizzas Artesanais</span> — Foz do Iguaçu / PR. Todos os direitos reservados.</p>
    </footer>

    <!-- SCRIPT SIMPLES PARA O ACORDEON DAS PERGUNTAS FREQUENTES -->
    <script>
        document.querySelectorAll('.faq-question').forEach(button => {
            button.addEventListener('click', () => {
                const item = button.parentElement;
                const isActive = item.classList.contains('active');
                
                // Fecha todos os outros itens
                document.querySelectorAll('.faq-item').forEach(el => {
                    el.classList.remove('active');
                });

                // Se não estava ativo, abre este
                if (!isActive) {
                    item.classList.add('active');
                }
            });
        });
    </script>
</body>
</html>
